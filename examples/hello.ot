// hello.ot — The simplest Orbitron program
// Prints a greeting and demonstrates basic variables.

func main() {
    println("Hello, World!");

    var x = 42;
    println(x);

    var pi: float = 3.14159;
    println(pi);

    var summa = x + 8;
    println(summa);
}
