// examples/01_basics/variables.ot
// Демонстрирует: переменные, типы, константы, переприсвоение,
// составное присваивание, оператор **.

// Константы верхнего уровня — доступны всем функциям
#const GRAVITY:   i64 = 10;     // м/с²
#const MASS:      i64 = 75;     // кг
#const MAX_SPEED: i64 = 300;    // км/ч
#const PI_INT:    i64 = 3;      // приближение π

fn main() {
    println!("=== Переменные и типы ===");

    // --- Целые числа ---
    let height:   i64 = 100;   // метры
    mut velocity: i64 = 0;
    let count         = 10;    // тип выводится как i64

    println!("height = \{height}");
    println!("count  = \{count}");

    // --- Числа с плавающей точкой ---
    let ratio: f64 = 1.618;
    let temp        = 36.6;   // тип выводится как f64

    println!("ratio = \{ratio}");
    println!("temp  = \{temp}");

    // --- Булевы значения (true=1, false=0) ---
    let is_positive = true;    // == 1
    let is_done     = false;   // == 0

    println!("is_positive = \{is_positive}");
    println!("is_done     = \{is_done}");

    // --- Переприсвоение (только mut-переменные) ---
    velocity = 30;
    println!("velocity = \{velocity}");

    // --- Составное присваивание ---
    mut total = 0;
    total += 10;   // total == 10
    total += 20;   // total == 30
    total -= 5;    // total == 25
    total *= 2;    // total == 50
    total /= 5;    // total == 10
    println!("total = \{total}");

    // --- Оператор ** (степень) ---
    let v_sq = velocity ** 2;    // 900
    let kb   = 2 ** 10;          // 1024 = 1 килобайт
    let mb   = 2 ** 20;          // 1048576 = 1 мегабайт

    println!("velocity^2 = \{v_sq}");
    println!("1 KB = \{kb} байт");
    println!("1 MB = \{mb} байт");

    // --- Использование констант ---
    let force  = MASS * GRAVITY;            // 750 Н
    let energy = MASS * GRAVITY * height;   // J

    println!("Сила: \{force} Н");
    println!("Потенциальная энергия: \{energy} Дж");

    // --- Деление ---
    let a = 10;
    let b = 3;
    let q = a / b;     // 3 (целочисленное)
    let r = a % b;     // 1 (остаток)

    println!("\{a} / \{b} = \{q} (остаток \{r})");

    // --- Числа с плавающей точкой ---
    let fd: f64 = 10.0;
    let result = fd / 3.0;    // 3.333...
    println!("10.0 / 3.0 = \{result}");
}
